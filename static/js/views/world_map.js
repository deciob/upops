// Generated by CoffeeScript 1.4.0

define(['backbone', 'text!templates/world_map.html'], function(Backbone, template) {
  'use strict';

  var WorldMap;
  return WorldMap = Backbone.View.extend({
    el: "#world_map",
    initialize: function(options) {
      options = options || {};
      this.defaultMessage = "World Map (main visualisation)";
      this.message = options.message || this.defaultMessage;
      return this.setElement($(this.el));
    },
    render: function() {
      template = _.template(template, {
        message: this.message
      });
      return this.$el.html(template);
    }
  });
});
